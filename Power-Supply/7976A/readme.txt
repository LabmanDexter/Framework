Note, the regression test data stored here serves as verification that the xPFA BTE #1 Baseline data

The test station has multiple PFS Slices installed to test every interface. 

The naming convention of the slice/assembly configuration has not really been agreed upon, so the UUT model and serial number vary.

However, the LPFA and DPFA test data are extracted from the same BTE and UUT Configuration.

